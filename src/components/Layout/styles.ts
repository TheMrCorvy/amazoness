import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles({
	mainContainer: {
		paddingTop: "7rem",
		paddingBottom: "7rem",
		minHeight: "80vh",
	},
	footer: {
		textAlign: "center",
	},
})
